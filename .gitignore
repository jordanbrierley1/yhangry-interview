# dependencies / build artifacts
node_modules
dist
coverage
.vite

# local SQLite database (regenerated by `pnpm bootstrap`)
apps/api/prisma/dev.db
apps/api/prisma/dev.db-journal

# environment / secrets
.env
.env.*
*.local

# OS / logs
.DS_Store
*.log

# local notes — not part of the candidate project
docs/
